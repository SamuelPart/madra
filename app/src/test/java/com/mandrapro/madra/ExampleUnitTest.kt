package com.mandrapro.madra

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Test unitario de humo: verifica que el modulo :app compila y que la
 * infraestructura de tests esta correctamente cableada.
 */
class ExampleUnitTest {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
